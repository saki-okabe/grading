# 第3回課題（プレ）UML：採点基準

## 1. 課題の目的

大学祭の出店企画について、参考となる現行システムまたはサービスを一つ選び、その現状をUML図で分析する。

指定された作業は次の3種類である。

1. 参考となる現行システムまたはサービスを決める
2. そのシステムまたはサービスのユースケース図を作成する
3. そのシステムまたはサービスのアクティビティ図を作成する

## 2. 採点方針

このプレ課題は2点満点とする。内容の良し悪し、UML記法の細かな正確性、図の完成度、見た目は点数に使用しない。指定された3種類の作業に取り組んでいるかだけを判定する。

- 3種類すべてに取り組んでいる：2点
- 1種類または2種類に取り組んでいる：1点
- 3種類とも確認できない：0点
- 提出なし：0点
- PDF以外：作業点から1点減点
- 遅延：減点なし

最終点は次の式で計算する。

```text
点数 = max(0, 作業点 + PDF形式減点)
```

## 3. 観察項目で使用できる値

P1～P3は、必ず次のいずれかで判定する。

- `Yes`：該当する作業に取り組んだ形跡が確認できる。
- `No`：該当する作業の形跡が確認できない。
- `判定不能`：ページ不足、不鮮明、見切れなどにより判断できない。

`Partial`は使用しない。

---

## 4. 観察項目

### P1_現行システムまたはサービスの選定

参考とする現行システムまたはサービスを決める作業である。

次のいずれかが確認できれば`Yes`とする。

- 現行システムまたはサービスの名称が書かれている。
- 「参考システム」「現行システム」「対象サービス」などの記載がある。
- 待ち時間管理システムなど、分析対象が文章または見出しで特定できる。
- 後続する図の表題から、対象システムまたはサービスが明確に分かる。

名称の選択が適切か、大学祭企画との関連が十分かは点数に使用しない。

- 単に「システム」とだけ書かれ、何を対象にしたのか分からない：`No`
- 分析対象を示す記載がない：`No`
- 画像が不鮮明、見切れまたはページ不足で名称を読めない：`判定不能`

### P2_ユースケース図への取組

ユースケース図とは、システムまたはサービスを利用する人や外部の役割と、その人がシステムを通じて行うことを表す図である。

一般的には、次のような要素がある。

- 人型などで表された利用者、運営者、管理者などのアクター
- 利用者が行う操作や、システムが提供する機能
- アクターと機能を結ぶ線
- 必要に応じてシステムの範囲を示す囲み

次のいずれかが確認できれば`Yes`とする。

- 「ユースケース図」という見出しと図がある。
- 利用者、管理者、出店者などの役割と、利用する機能が図示されている。
- 人型のアクターと複数の機能が線で結ばれている。
- 一般的なUML記法と多少異なっていても、アクターと機能の関係を図にしようとした形跡がある。

次の記法上の誤りがあっても、取組を確認できれば`Yes`とする。

- ユースケースが楕円で書かれていない。
- システム境界がない。
- 関連線が一部不足している。
- アクター名や機能名が不十分である。
- `include`、`extend`などを使用していない。

- 見出しだけで図がない：`No`
- アクターと機能のどちらも確認できず、単なる文章一覧だけである：`No`
- 図はあるが、ユースケース図か判断できない：`判定不能`
- 不鮮明、見切れまたはページ不足で判断できない：`判定不能`

### P3_アクティビティ図への取組

アクティビティ図とは、システムやサービスにおける処理、作業、行動の流れを表す図である。

一般的には、次のような要素がある。

- 開始位置
- 処理や行動を表す項目
- 処理の順序を結ぶ矢印
- 条件による分岐
- 終了位置
- 必要に応じて役割別の領域

次のいずれかが確認できれば`Yes`とする。

- 「アクティビティ図」という見出しと図がある。
- 複数の処理や行動が矢印で結ばれている。
- 開始から複数の処理を経て終了へ向かう流れを図示しようとしている。
- 一般的なUML記法と多少異なっていても、処理の流れを図にしようとした形跡がある。

次の記法上の誤りがあっても、取組を確認できれば`Yes`とする。

- 開始ノードがない。
- 終了ノードがない。
- 分岐記号が正確でない。
- 矢印が一部不足している。
- 役割別の領域がない。
- 処理の流れに不明確な部分がある。

- 見出しだけで図がない：`No`
- 処理の流れがなく、単語や機能を並べただけである：`No`
- 図はあるが、アクティビティ図か判断できない：`判定不能`
- 不鮮明、見切れまたはページ不足で判断できない：`判定不能`

---

## 5. 誤判定の防止

- 同じ1つの図を、根拠なくP2とP3の両方に数えない。
- 図の見出し、記号、矢印、説明文、ページ構成を合わせて判断する。
- ユースケース図は「誰が何を利用するか」を表す図として識別する。
- アクティビティ図は「どのような順序で処理するか」を表す図として識別する。
- UMLとして正確でなくても、該当する図を作成しようとした形跡があれば`Yes`とする。
- 見出しだけで図がなければ`No`とする。
- 不鮮明、見切れ、ページ不足の場合は`判定不能`とする。
- 学生の意図を根拠なく推測しない。

---

## 6. 作業点

AIの観察と点数計算を分ける。最初にP1～P3をすべて判定し、その後で`Yes`の個数を数えて作業点を機械的に決定する。

| Yesの個数 | 作業点 |
|---:|---:|
| 3個 | 2点 |
| 1個または2個 | 1点 |
| 0個かつP1～P3がすべてNo | 0点 |

- 提出物がない場合は0点とする。ただし、提出なしはVLMではなく、`grader.py`の提出物走査処理で判定する。
- 図の内容やUML記法の誤りを理由に、作業点を下げない。

## 7. 判定不能の処理

`判定不能`を`No`として扱わない。

P1～P3に`判定不能`が1つ以上ある場合は、次の順序で処理する。

1. `Yes`と確認できた項目数だけで暫定作業点を計算する。
2. `暫定点`を`true`にする。
3. `人間確認必要`を`true`にする。
4. 判断できなかった項目名と理由を`人間確認理由`に記載する。

`Yes`が0個で、1項目以上が`判定不能`の場合、暫定作業点は0点とする。人間確認後に点数が上がる可能性があることを`評価理由`に記載する。

例：P1=`Yes`、P2=`判定不能`、P3=`No`の場合、確認できた`Yes`は1個なので暫定作業点は1点とする。人間確認後に2点へ変更される可能性がある。

## 8. PDF形式

提出形式は答案画像からVLMに推測させない。将来的に`grader.py`が、実際に採点対象として選択した元ファイルの拡張子から判定する。

- PDF：`PDF形式減点`は0
- PDF以外：`PDF形式減点`は-1
- 提出なし：`点数`は0
- 遅延：減点なし

この採点基準をVLMだけで使用し、元ファイル形式がプロンプト等で明示されていない場合、VLMは形式を推測せず、`PDF形式減点`を暫定的に0とする。形式確認がPython側で必要であることを`人間確認理由`に記載し、`暫定点`と`人間確認必要`をともに`true`にする。

最終点は次の式で計算する。

```text
点数 = max(0, 作業点 + PDF形式減点)
```

---

## 9. フィードバック

プレ課題では、基本的に不足している作業だけを簡潔に伝える。

使用できる例：

- 「現行システムの選定、ユースケース図、アクティビティ図の3つへの取組が確認できました。」
- 「参考とする現行システムまたはサービスを明記してください。」
- 「ユースケース図への取組が確認できませんでした。」
- 「アクティビティ図への取組が確認できませんでした。」
- 「PDF形式で提出してください。」

UML記法上の誤りは点数に反映しない。ただし、最終課題に向けた参考情報として、画像から明確に確認できる不足を1つだけフィードバックに記載してもよい。判定不能の項目を「存在しない」と断定しない。

---

## 10. 出力JSONテンプレート

説明文、前置き、Markdownのコードブロックを付けず、次のキーを一字一句変えずにJSONだけを返すこと。

```json
{
  "点数": 2,
  "暫定点": false,
  "観察項目": {
    "P1_現行システムまたはサービスの選定": "Yes",
    "P2_ユースケース図への取組": "Yes",
    "P3_アクティビティ図への取組": "Yes"
  },
  "観点別評価": {
    "取組状況": "指定された3種類の作業すべてへの取組が確認できる",
    "提出形式": "PDF形式で提出されている"
  },
  "配点内訳": {
    "作業点": 2,
    "PDF形式減点": 0
  },
  "減点理由": [],
  "評価理由": "現行システムの選定、ユースケース図、アクティビティ図への取組が確認されたため、作業点は2点",
  "フィードバック": "",
  "人間確認必要": false,
  "人間確認理由": []
}
```

## 11. JSONの制約

- `点数`は0、1、2のいずれかとする。
- `暫定点`と`人間確認必要`はJSONのbooleanである`true`または`false`とし、文字列にしない。
- P1～P3は`Yes`、`No`、`判定不能`のいずれかとする。
- `Partial`は使用しない。
- `作業点`は0、1、2のいずれかとする。
- `PDF形式減点`は0または-1とする。
- `点数`は`max(0, 作業点 + PDF形式減点)`と一致させる。
- 提出なしは0点とする。
- P1～P3に`判定不能`が1つでもあれば`暫定点`を`true`にする。
- P1～P3に`判定不能`が1つでもあれば`人間確認必要`を`true`にする。
- 元ファイル形式が確定していない場合も、`暫定点`と`人間確認必要`を`true`にする。
- `人間確認必要`が`true`なら、`人間確認理由`を空配列にしない。
- 遅延による減点項目を作らない。
- UMLの内容の良し悪しを点数へ反映しない。
- 同じ不足を重複して減点しない。
- 失点またはPDF形式減点がある場合は、該当項目と理由を`減点理由`に記載する。
- `評価理由`には、`Yes`の個数、作業点、PDF形式減点、最終点の計算を記載する。
